{{#each errors}}
  <div class="alert alert-danger">
    {{text}}
  </div>
{{/each}}

<div class="card card-body">
  <h3>{{title}}</h3>
  <form action={{action}} method="post">
    {{> _form }}
  </form>
</div>